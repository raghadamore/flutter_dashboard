class BusinessProjectModel {
  String name;
  String email;
  DateTime dateTime;
  String gender;
  String lastAddress;
  String currentAddress;
  String phone;
  BusinessProjectModel({required this.phone,required this.name, required this.email, required this.dateTime,required this.gender,required this.lastAddress,required this.currentAddress});
}
